image:
  repository: ghcr.io/zynthasius39/gopher
  pullPolicy: IfNotPresent
  tag: 1.0.0

ingress:
  enabled: false
  className: cilium
  annotations: {}
  hosts: []
  tls: []

hpa:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

replicaCount: 1

resources: {}

service:
  type: ClusterIP
  port: 8080

serviceMonitor:
  enabled: false
