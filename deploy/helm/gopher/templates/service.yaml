apiVersion: v1
kind: Service
metadata:
  name: {{ include "gopher.fullname" . }}
  labels:
    {{- include "gopher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gopher.selectorLabels" . | nindent 4 }}
