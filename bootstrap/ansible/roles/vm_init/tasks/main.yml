---
- name: SSH Keygen
  include_tasks: ssh.yml

- name: Virt Install
  include_tasks: install.yml
  loop: "{{ nodes }}"
  loop_control:
    loop_var: node

# - name: Virt Install (virt-install cmd)
#   include_tasks: install_cmd.yml
#   loop: "{{ nodes }}"
#   loop_control:
#     loop_var: node
